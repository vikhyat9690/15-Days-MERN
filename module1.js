const sum = function(a, b) {
    console.log(a + b);
}
//  sum(20, 3);

const mul = function mul(a, b) {
    console.log(a * b);
 }

// mul(30, 10);

module.exports = {
    sum,
    mul,
};